function getNotes() {
    return 'Your notes...'
}

const AddNote = (title, body) => {
    const notes = LoadNotes()
}

const LoadNotes = () => {

}

module.exports = {
    getNotes: getNotes,
    addNote: AddNote
};